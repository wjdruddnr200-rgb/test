[project]
name = "flight-search-meta"
version = "0.1.0"
description = "Compare flight search results across multiple providers based on date/time/price constraints."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
flight-search = "flight_search.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
